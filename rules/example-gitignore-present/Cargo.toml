[package]
name = "example-gitignore-present"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "iderm-doctor-rules:example-gitignore-present"
target = { world = "doctor-plugin", path = "wit/deps/iderm-plugin/iderm-plugin.wit" }

[package.metadata.component.dependencies]
